# Runtime
PySide6==6.11.1
Pymem==1.14.0        # fallback memory backend if farever_native isn't built
pillow==12.2.0

# Build / dev (not needed at runtime)
maturin==1.13.3      # builds the Rust extension (farever_native)
pyinstaller==6.20.0  # one-file packaging
pytest==9.0.3        # headless tests
